'use strict';

var test = require('ava');

var one = ' Write a function that loops through the numbers n down to 0. If you haven\'t done so try using a while loop to do this. ';

function loopN(n) {
    console.log(n);
    if (n <= 0) {
        return n;
    }
    loopN(n - 1);
}

test(one, function (t) {
    var act = loopN(3);
    var exp = 0;

    t.pass();
});

function exponent(base, expo) {}

//3.Write a function 'exponent' that takes two arguments base, and expo, uses a while loop to return the exponenet value of the base.

test.only('exponent', function (t) {

    exponent(3, 3);
});
//# sourceMappingURL=node_modules/.cache/ava/2069f3f936c2e5aaf8daaa3b2515caf8.js.map